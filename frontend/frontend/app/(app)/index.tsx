/**
 * Home route shim — the actual implementation lives in
 * `src/screens/home/HomeScreen.tsx` (decomposed into components/ +
 * hooks/). Keep this file as the expo-router entry point only.
 */
export { default } from "@/src/screens/home/HomeScreen";
