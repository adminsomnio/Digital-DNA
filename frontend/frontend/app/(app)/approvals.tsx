/**
 * Approvals route shim. The screen itself lives under
 * `src/screens/approvals/` so the route file stays a thin entry point.
 */
import ApprovalsScreen from "@/src/screens/approvals/ApprovalsScreen";

export default ApprovalsScreen;
