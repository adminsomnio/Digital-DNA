/**
 * Per-order CAD files route shim. Implementation lives in
 * `src/screens/cad-files/CadFilesScreen.tsx` (decomposed into
 * components/ + hooks/).
 */
export { default } from "@/src/screens/cad-files/CadFilesScreen";
