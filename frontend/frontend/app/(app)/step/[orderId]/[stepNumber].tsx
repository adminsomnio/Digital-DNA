/** Step route shim — the 26-step workflow screen body lives under
 *  src/screens/step/. Keep this file as a single-line export so future
 *  refactors of the screen don't touch the route tree. */
import StepScreen from "@/src/screens/step/StepScreen";

export default StepScreen;
